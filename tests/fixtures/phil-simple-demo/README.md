# phil-simple-demo

Generated by cli2ansible

## Description

This role was automatically generated from terminal session commands.

## Tasks

This role contains 4 tasks:

1. Run: echo "Hello Phillip" (module: `shell`, confidence: low)
2. Create directory: test_1 (module: `file`, confidence: high)
3. Run: cd test_1 (module: `shell`, confidence: low)
4. Run: exit (module: `shell`, confidence: low)

## Usage

```yaml
- hosts: all
  roles:
    - phil-simple-demo
```

## Testing

Run with Ansible playbook:

```bash
ansible-playbook -i "localhost," -c local test-playbook.yml
```

Or run Molecule tests:

```bash
molecule test
```
